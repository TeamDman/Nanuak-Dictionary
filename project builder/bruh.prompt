Implement all todo items and return the new content in full.

/// Read all files like "../project description/*" and return them as a concatenated string.
/// Fence each file in a markdown code block.
/// Prepend each code block with the relative path to the file.
pub fn get_description() -> eyre::Result<String> {
    todo!()
}
